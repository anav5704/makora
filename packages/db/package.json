{
    "name": "@makora/db",
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./src/index.ts"
        },
        "./*": {
            "types": "./dist/*.d.ts",
            "default": "./src/*.ts"
        }
    },
    "scripts": {
        "build": "tsdown",
        "db:studio": "cd libsql && prisma studio",
        "db:seed": "cd libsql && prisma db seed",
        "db:push": "cd postgres && prisma db push && cd ../libsql && prisma db push",
        "db:generate": "cd postgres && prisma generate && cd ../libsql && prisma generate"
    },
    "devDependencies": {
        "prisma": "catalog:prisma",
        "tsdown": "catalog:tsdown"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@prisma/adapter-libsql": "^7.0.1",
        "@prisma/adapter-pg": "^7.0.1",
        "@prisma/client": "catalog:prisma",
        "dotenv": "catalog:dotenv",
        "pg": "^8.16.3"
    }
}
